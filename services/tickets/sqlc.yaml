version: "2"
sql:
  - engine: postgresql
    schema: postgresql/migrations
    queries: sql
    gen:
      go:
        package: sql
        out: gen/sql
        sql_package: pgx/v5
        emit_interface: true
        emit_empty_slices: true
