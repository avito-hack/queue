version: "2"

sql:
  - engine: postgresql

    schema:
      - ./postgresql/migrations

    queries:
      - ./postgresql/queries

    gen:
      go:
        package: sqlc
        out: ./postgresql/sqlc

        sql_package: pgx/v5

        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: true